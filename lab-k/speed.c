int main(void) {
  // Declare variables
  double distance = 0, time = 0;
  int speed = 0;

  // Set variables
  distance = 100.0; // meters
  time = 9.58;      // seconds

  // Calculate speed
  speed = distance / time; // speed in m/s

  return 0;
}